//78. Subsets
//^1. array as ref assign if temp change later output also change later ==> so use spread operator
//? output[...temp]

//^ 2. output not put in global scope have function scope --> no clean up for next test case ==> value remain same 
//?because function call mai args change ==> global remain same