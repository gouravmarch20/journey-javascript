// search => stop futher itteration if one item find
//? return index if found , if not found return -1
const arr = [4, 'git', 55]
console.log(arr.findIndex(i => i === 55))
console.log(arr.findIndex(i => i === 'git'))
console.log(arr.findIndex(i => i === 'gitman'))
