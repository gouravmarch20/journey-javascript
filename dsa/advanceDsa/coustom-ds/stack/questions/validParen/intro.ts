//20. Valid Parentheses => easy

// logic
// ! m1 : ignore open brackets must be closed in the correct order.
// ! m2 : ']' case ignored what if only close bracket ==> if we are not pushing in stack then stack length 0 

// silly mistake 
// ~ m1> : miss and operator   if(top === '(' !== s[i] === ')' )


