// 50. Pow(x, n)

// !m1 if power 1 -> same number return
//!m2  if power 0 ->return 1
//^ m3: when power is 1 stop , that the result so return at odd no


/** way1 : looping
 //? O(n) 
// pow -> if power +ve  only then ->  simple loop
// pow -> if -ve :
   //^  x ^ (- n) ==> 1 / x ^ n
   //? but if x is negative  , if we change negative to +ve  ==> overflow
   // long use 
 */
/** way2 : 

 */
