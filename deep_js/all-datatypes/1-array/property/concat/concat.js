// use to combine arrays & return new array
const number = [3, 4]
const alphabet = ['a', 'b', 'c']
const numAlpha = [1 ,'a']
const combineArray = number.concat(alphabet , numAlpha)
console.log(combineArray)
console.log(number)// no change
console.log(alphabet)// no change
